# Compiler-first public reset

This branch supersedes the ontology-first homepage draft. It restores a green production baseline, presents Idol as a Lua-derived compiler, limits featured source to compiler evidence, revalidates stable assets, and requires real-browser mobile and desktop admission before deployment.
