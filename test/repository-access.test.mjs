import test from "node:test";import assert from "node:assert/strict";import {attachRepositoryAccess,provisionRepositoryAccess} from "../scripts/repository-access-lib.mjs";
const destinations=[{type:"public",uri:"platform.idol.id/repo*"},{type:"public",uri:"platform.idol.id/v1/repository/browser/*"}];
function reply(result,status=200){return new Response(JSON.stringify({success:status>=200&&status<300,result,errors:[]}),{status,headers:{"content-type":"application/json"}})}
function fetcher(state,calls){return async(url,init={})=>{const path=new URL(url).pathname,method=init.method||"GET",body=init.body?JSON.parse(init.body):null;calls.push({path,method,body});if(path.endsWith("/access/identity_providers"))return reply([{id:"otp",type:"onetimepin"}]);if(path.endsWith("/access/apps")&&method==="GET")return reply(state.app?[state.app]:[]);if(path.endsWith("/access/apps")&&method==="POST"){state.app={...body,id:"repo-app",aud:"repo-aud"};return reply(state.app)}if(path.endsWith("/access/apps/repo-app")&&method==="PUT"){state.app={...body,id:"repo-app",aud:"repo-aud"};return reply(state.app)}if(path.endsWith("/access/apps/repo-app/policies")&&method==="GET")return reply(state.policy?[state.policy]:[]);if(path.endsWith("/access/apps/repo-app/policies")&&method==="POST"){state.policy={id:"policy",...body};return reply(state.policy)}throw new Error(`unexpected ${method} ${path}`)}}
test("repository Access is exact and idempotent",async()=>{const state={},calls=[];const first=await provisionRepositoryAccess({accountId:"account",apiToken:"token",bootstrapEmail:"owner@example.com",fetcher:fetcher(state,calls)});assert.equal(first.audience,"repo-aud");assert.deepEqual(state.app.destinations,destinations);assert.deepEqual(state.policy.include,[{email:{email:"owner@example.com"}}]);calls.length=0;const second=await provisionRepositoryAccess({accountId:"account",apiToken:"token",bootstrapEmail:"owner@example.com",fetcher:fetcher(state,calls)});assert.equal(second.audience,first.audience);assert.equal(calls.some(x=>["POST","PUT"].includes(x.method)),false);});
test("repository Access refuses unrelated destination drift",async()=>{const state={app:{id:"repo-app",aud:"repo-aud",name:"Idol Repository Observatory",type:"self_hosted",destinations:[...destinations,{type:"public",uri:"admin.example.com/*"}],session_duration:"24h",allowed_idps:["otp"],auto_redirect_to_identity:true,app_launcher_visible:false,skip_interstitial:true,custom_deny_message:"This private Idol repository surface requires the admitted owner identity."}};await assert.rejects(()=>provisionRepositoryAccess({accountId:"account",apiToken:"token",bootstrapEmail:"owner@example.com",fetcher:fetcher(state,[])}),/unknown destination/);});
test("production config receives only repository Access audience",()=>{const config=attachRepositoryAccess({vars:{IDOL_AUTHORITY:"authority"}},{audience:"repo-aud"});assert.deepEqual(config.vars,{IDOL_AUTHORITY:"authority",REPOSITORY_ACCESS_AUD:"repo-aud"});assert.equal(JSON.stringify(config).includes("token"),false);});
